#include "main.h"
/**
 * sum - a function that adds two integers and returns the result
 * a: first number input
 * b: second number input
 * Return: the sum of a and b
 */
int add(int a, int b)
{
	return (a + b);
}
